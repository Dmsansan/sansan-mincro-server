spring.application.name=ribbon-server
server.port=8082

#producers.ribbon.listOfServers=localhost:8066, localhost:8067
#
#management.server.port=9001

#开启所有页面节点 默认只开启health info两个页面节点
management.endpoints.web.exposure.include="*"

spring.security.user.name=admin
spring.security.user.password=123456

info.app.name=ribbon-server
info.company.name=www.sisansan.com
info.build.artifactId=ribbon-server
info.build.version=1.0.0

eureka.client.service-url.defaultZone=http://${spring.security.user.name}:${spring.security.user.password}@localhost:3030/eureka/
eureka.instance.prefer-ip-address=true
eureka.instance.instance-id=${spring.cloud.client.ip-address}:${server.port}
eureka.instance.lease-renewal-interval-in-seconds=1
eureka.instance.lease-expiration-duration-in-seconds=2

logging.path=logs
logging.file=ribbonServer